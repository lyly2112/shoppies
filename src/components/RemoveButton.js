// import React, { useState, useEffect } from "react";
// // import FormControlLabel from "@material-ui/core/FormControlLabel";
// import Button from "@material-ui/core/Button";

// import Clear from "@material-ui/icons/Clear";

// // import { ListItemSecondaryAction, MuiThemeProvider } from "@material-ui/core";

// function RemoveButton({ nominations, setNominations }) {
//   const removeNomination = (event) => {
//     if (event.target.onClick) {
//       const newList = list.filter((nomination) => nomination.imdbID !== imdbID);
//       setNominations(newList);
//       console.log("nominations new list is  ", newList);

//       console.log(
//         "Nominate component -> nominations after setNominations remove button ",
//         nominations
//       );
//     }
//   };
//   return <Button onClick={removeNomination(nomination.imdbID)}>{<Clear />} </Button>;
// }

// export default RemoveButton;
